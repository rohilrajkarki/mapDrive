import { View, Text } from "react-native";
import React from "react";
import MapView from "react-native-maps";

const Maps = () => {
  return (
    <MapView>
      <Text>Maps</Text>
    </MapView>
  );
};

export default Maps;
